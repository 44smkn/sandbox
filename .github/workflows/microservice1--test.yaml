name: microservice1-log / test
on:
  push:
    paths:
      - .github/workflows/microservice1--test.yaml
    branches-ignore:
      - main

jobs:
  golangci:
    name: lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: golangci-lint
        uses: golangci/golangci-lint-action@v2
        with:
          version: v1.40.1
